<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>Join Club</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="stylesheets/style.css">
        <link rel="stylesheet" href="stylesheets/join-club.css">
    </head>
    <body>
        <div class="mainBodyContainer">
            <%- include("./partials/header") %>
            <div class="joinClubContainer">
                <% if (currentUser) { %>
                    <h1>Join Club</h1>
                    <form method="post" class="joinClubForm">
                        <input type="password" name="code" placeholder="Passcode" required>
                        <input type="submit" hidden />
                        <button type="submit" class="defaultButton">Submit</button>
                    </form>
                    <% if(error) {%>
                        <div><%=error%></div>
                    <% } %>
                <% } else { %>
                    <div>Must be signed in</div>
                    <a href="/sign-in" class="redirectLink">Sign In</a>
                <% } %>
            </div>
            <%- include("./partials/footer") %>
        </div>
    </body>
</html>